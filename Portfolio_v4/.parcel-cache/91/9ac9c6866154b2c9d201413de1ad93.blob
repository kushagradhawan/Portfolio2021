var _parcelHelpers = require("@parcel/transformer-js/lib/esmodule-helpers.js");
_parcelHelpers.defineInteropFlag(exports);
function nearest16(value, integer) {
  console.log(value.isInteger && integer.isInteger);
  if (!(value.isInteger && integer.isInteger)) return;
  if (value === undefined || integer === undefined) return;
  const tempVal = Math.ceil(value / integer) * integer;
  return tempVal - value < integer / 2 ? tempVal + integer : tempVal;
}
exports.default = nearest16;
/*simple test case*/
let testArray = [];
for (let i = 0; i < 100; i++) {
  let a = Math.floor(Math.random() * 100);
}
console.log(nearest16(2, 16));
