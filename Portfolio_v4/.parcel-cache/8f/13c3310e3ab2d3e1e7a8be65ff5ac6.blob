var _parcelHelpers = require("@parcel/transformer-js/lib/esmodule-helpers.js");
_parcelHelpers.defineInteropFlag(exports);
function nearestNextInteger(value, integer) {
  /*add sanity checks here*/
  console.log(Math.ceil(value / integer) * integer - integer, integer);
  return Math.ceil(value / integer) * integer - integer;
}
exports.default = nearestNextInteger;
/*simple test case*/
let testArray = [];
for (let i = 0; i < 100; i++) {
  let a = Math.floor(Math.random() * 100);
  nearestNextInteger(a, 16);
}
